# Assets

Images, diagrams, and other binary files referenced by the documentation.

Reference them from `docs/` with a root-relative path, for example
`![Diagram](../assets/example.png)`. Keep files small — `check-added-large-files`
rejects anything over 2 MB.
